class ExploreModel {
  const ExploreModel({
    required this.imageUrl,
    required this.title,
    required this.kmAway,
    required this.date,
    required this.price,
    required this.points,
  });

  final String imageUrl, title, kmAway, date, price, points;

}
