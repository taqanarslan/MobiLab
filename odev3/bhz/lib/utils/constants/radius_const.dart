import 'package:flutter/material.dart';

class RadiusConst{
  static  BorderRadius textInput = BorderRadius.circular(8);
  static  BorderRadius image = BorderRadius.circular(15);
  static  BorderRadius containerRadius = BorderRadius.circular(40);
}